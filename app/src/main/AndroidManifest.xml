<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectonavigation">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SportsPreferencesActivity"></activity>
        <activity android:name=".CardActivity" />
        <activity android:name=".ChangeProfileActivity" />
        <activity android:name=".CulturePreferencesActivity" />
        <activity android:name=".VideoGamesPreferencesActivity" />
        <activity android:name=".BooksPreferencesActivity" />
        <activity android:name=".TravelPreferencesActivity" />
        <activity android:name=".TVShowsPreferencesActivity" />
        <activity android:name=".MusicPreferencesActivity" />
        <activity android:name=".FoodPreferencesActivity" />
        <activity android:name=".CinemaPreferencesActivity" />
        <activity android:name=".PreferencesActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>